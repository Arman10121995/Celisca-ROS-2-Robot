# Per-robot navigation overlay for 'berkeley_humanoid_lite' (merged over controller_server.yaml / planner_server.yaml)
global_costmap:
  global_costmap:
    ros__parameters:
      robot_base_frame: base
      robot_radius: 0.3
local_costmap:
  local_costmap:
    ros__parameters:
      robot_base_frame: base
      robot_radius: 0.3
controller_server:
  ros__parameters:
    FollowPath:
      desired_linear_vel: 0.5
