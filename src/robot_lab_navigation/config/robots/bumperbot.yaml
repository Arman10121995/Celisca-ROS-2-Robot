# Per-robot navigation overlay for 'bumperbot' (merged over controller_server.yaml / planner_server.yaml)
global_costmap:
  global_costmap:
    ros__parameters:
      robot_base_frame: base_footprint
      robot_radius: 0.1
local_costmap:
  local_costmap:
    ros__parameters:
      robot_base_frame: base_footprint
      robot_radius: 0.1
controller_server:
  ros__parameters:
    FollowPath:
      desired_linear_vel: 0.3
