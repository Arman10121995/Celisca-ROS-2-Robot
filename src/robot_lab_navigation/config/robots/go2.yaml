# Per-robot navigation overlay for 'go2' (merged over controller_server.yaml / planner_server.yaml)
global_costmap:
  global_costmap:
    ros__parameters:
      robot_base_frame: base
      robot_radius: 0.35
local_costmap:
  local_costmap:
    ros__parameters:
      robot_base_frame: base
      robot_radius: 0.35
controller_server:
  ros__parameters:
    FollowPath:
      desired_linear_vel: 1.2
