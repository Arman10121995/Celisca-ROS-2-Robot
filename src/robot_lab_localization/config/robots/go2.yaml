# Per-robot localization overlay for 'go2' (merged over ekf.yaml / amcl.yaml)
ekf_filter_node:
  ros__parameters:
    base_link_frame: base
amcl:
  ros__parameters:
    base_frame_id: base
    scan_topic: scan
